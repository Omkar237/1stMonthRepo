Indexes are particularly useful for speeding up queries that involve searching, sorting, or filtering based on indexed columns.

In this scenario, where i have attached three png files the database engine can use the 'index1' index 
to quickly locate the rows where the location is 'City C', resulting in faster query performance.